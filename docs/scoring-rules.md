# Scoring rules

This document can be expanded by organizers if the workshop adopts a more formal scoring policy.

The current repository includes a simple reference implementation in `scripts/score_submissions.py`.

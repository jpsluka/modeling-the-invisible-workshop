# Participant predictions

Participants should place submission files here.

Suggested structure:

- `templates/` for blank or example submission files
- `submissions/` for team forecast files

Suggested file naming:

```text
submissions/<team_name>/<challenge_id>/<release_id>.csv
```

Submission files should follow the column layout in `templates/forecast_template.csv`.

# Scoring Scripts

This directory contains the validation and scoring scripts used by GitHub Actions and by local organizers.

- `validate_predictions.py` checks file format, week coverage, and consistency with the public release data.
- `score_predictions.py` computes round scores and the overall leaderboard using the reference answers file.

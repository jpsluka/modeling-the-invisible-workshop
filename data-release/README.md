# Data Release

Each round has its own folder:

```text
data-release/round-N/
```

Files in each round folder:

- `release_info.json` — defines the forecast horizon for that round
- `release.csv` — released hospitalization data through the public cutoff week
